{
    "version": 2,
    "builds": [
        {
            "src": "src/server.ts",
            "use": "@now/node",
            "command":"npx prisma generate"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"src/server.ts"
        }
    ]
}